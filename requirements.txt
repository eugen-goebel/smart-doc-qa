# Runtime dependencies for the Streamlit Cloud demo.
# pyproject.toml + uv.lock stay the source of truth for local dev and CI.
# This is the subset the app needs at runtime.
anthropic>=0.122.0
streamlit>=1.61.1
chromadb>=1.5.9
pypdf>=6.16.2
python-docx>=1.2.0
python-dotenv>=1.2.3
pydantic>=2.13.4

# Pin opentelemetry-proto and protobuf so chromadb's transitive
# tracing deps don't trip _CheckCalledFromGeneratedFile on Streamlit
# Cloud. The older opentelemetry-proto _pb2 files were generated with
# a protoc that's incompatible with protobuf 5.x.
opentelemetry-proto>=1.44.0
opentelemetry-exporter-otlp-proto-grpc>=1.44.0
protobuf>=7.36.0
